meta {
  name: Disable Year
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/food/disable-year
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Disable Year (Button 4 — when active with no suspension)

  Sets isActive=false on the FoodSubscription, disabling food entirely.
}
