meta {
  name: Undo Cancel Next Week
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/food/undo-cancel-week
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Undo Cancel Next Week (Button 1 — undo)

  Deletes the single-week FoodCancellation for the upcoming Monday,
  restoring next week's food. Available before Sunday 10:30 AM IST.
}
